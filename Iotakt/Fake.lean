import Iotakt.Fake.Poller

/-!
# Iotakt.Fake

Lean-only deterministic fake poller and test harness (RFC 008). Builds
without a C toolchain or Henret; it is the reference mechanism for
deterministic bridge tests.
-/
